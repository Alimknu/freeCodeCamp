To run any of the react code, from the FrontEnd_Development_Libraries_Certification directory run the following commands:

npm install react react-dom --save
npm install @types/react @types/react-dom --save-dev

If you don't have npm, you need to install it